pipelines:
  - name: test
    configuration:
      inputSteps:
        - name: setup
    execution:
      onExecute:
        - pushd ./setup/app
        - MODULE="$module" npm run test
        - popd
      onComplete:
        - save_tests ./setup/app/reports
